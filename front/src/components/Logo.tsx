import { components } from "../styles";

const Logo = () => (
  <div className={components.logo.container}>
    <span className={components.logo.first}>Vet</span>
    <span className={components.logo.second}>Helper</span>
  </div>
);

export default Logo;
